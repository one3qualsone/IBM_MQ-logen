# Elasticsearch Configuration
ES_URL=https://your-instance.es.cloud.es.io:443
ES_API_KEY=your-base64-encoded-api-key-here

# Index Configuration
INDEX_NAME=metrics-mq-demo

# Generation Mode: backfill or continuous
MODE=backfill

# Backfill Settings (only used when MODE=backfill)
BACKFILL_DAYS=30
BACKFILL_INTERVAL_MINUTES=1

# Continuous Settings (only used when MODE=continuous)
CONTINUOUS_SCENARIO=normal
CONTINUOUS_INTERVAL_SECONDS=60

# Available scenarios for continuous mode:
# - normal
# - gradual_degradation
# - critical_buildup
# - queue_full
# - spike
# - recovery